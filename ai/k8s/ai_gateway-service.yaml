apiVersion: v1
kind: Service
metadata:
  name: ai-gateway-svc
  namespace: default
  labels:
    app: ai-gateway
spec:
  selector:
    app: ai-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
